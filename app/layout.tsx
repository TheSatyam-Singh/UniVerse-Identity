import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'UniVerse Identity - Secure Authentication Provider',
  description: 'Production-ready authentication and identity provider for universelabs.tech',
  keywords: ['authentication', 'oauth2', 'openid connect', 'identity provider'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
